{
  "name": "@rari/monorepo",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean",
    "lint": "pnpm -r run lint && oxlint crates/ && eslint crates/ packages/ web/ examples/",
    "lint:fix": "pnpm -r run lint:fix && oxlint crates/ --fix && eslint crates/ packages/ web/ examples/ --fix",
    "lint:knip": "knip"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:eslint",
    "@e18e/eslint-plugin": "catalog:",
    "@eslint-react/eslint-plugin": "catalog:eslint",
    "@types/node": "catalog:",
    "@typescript-eslint/utils": "catalog:",
    "@typescript/native-preview": "catalog:",
    "eslint": "catalog:eslint",
    "eslint-plugin-de-morgan": "catalog:",
    "eslint-plugin-oxlint": "catalog:eslint",
    "eslint-plugin-react-hooks": "catalog:eslint",
    "eslint-plugin-react-refresh": "catalog:eslint",
    "knip": "catalog:eslint",
    "oxlint": "catalog:"
  }
}
