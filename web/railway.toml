[build]
builder = "RAILPACK"
buildCommand = "cd .. && pnpm install --frozen-lockfile && pnpm --filter @rari/web build"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
